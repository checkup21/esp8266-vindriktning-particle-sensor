#pragma once

namespace Config {

char *wifi_SSID = "<SSID>";
char *wifi_password = "<wifi password>";
char *mqtt_server = "10.0.0.80";

// leave empty for anonymous access servers
char *username = "";
char *password = "";

} // namespace Config
